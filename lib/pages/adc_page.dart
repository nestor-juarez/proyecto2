import 'package:flutter/material.dart';

class ADCPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Aseguramiento de la Calidad'),
          centerTitle: true,
          backgroundColor: Color(0xFF0077B6),
        ),
      );
}
